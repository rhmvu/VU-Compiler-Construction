extern int printf(char[] format, ...);

int main()
{
    int i = 0;
    int j = 0;
    while (i < 9) {
        i += 1;
        //1 and 2 adn 3 only
       
            do {
		while(j<3) {
			j+=1;
                    printf("deepest dowhile int i:%d\n", i);
                    break;
                }
                break;
            } while (1==1);
	printf("%d\n", i);
        if (i > 7)
            continue;
    }
	return 0;
}
